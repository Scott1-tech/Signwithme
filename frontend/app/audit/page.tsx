import { AuditScreen } from "@/components/audit/audit-screen";

export const metadata = { title: "Audit log · Contract Review Desk" };

export default function AuditPage() {
  return <AuditScreen />;
}
