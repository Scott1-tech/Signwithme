import { SignaturesScreen } from "@/components/signatures/signatures-screen";

export const metadata = { title: "Signatures · Contract Review Desk" };

export default function SignaturesPage() {
  return <SignaturesScreen />;
}
