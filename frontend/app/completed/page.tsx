import { CompletedScreen } from "@/components/queue/completed-screen";

export const metadata = { title: "Completed · Contract Review Desk" };

export default function CompletedPage() {
  return <CompletedScreen />;
}
